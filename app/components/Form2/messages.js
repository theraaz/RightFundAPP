/*
 * Form2 Messages
 *
 * This contains all the text for the Form2 component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Form2';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Form2 component!',
  },
});
