/*
 * Singup Messages
 *
 * This contains all the text for the Singup container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Singup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Singup container!',
  },
});
