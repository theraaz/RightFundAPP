/*
 * CampaignCreate Messages
 *
 * This contains all the text for the CampaignCreate container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CampaignCreate';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CampaignCreate container!',
  },
});
